diff --git cef/BUILD.gn cef/BUILD.gn
index 6d0f2f1..4c3f1d1 100644
--- cef/BUILD.gn
+++ cef/BUILD.gn
@@ -668,6 +668,7 @@ source_set("libcef_static") {
     "libcef/common/task_runner_impl.h",
     "libcef/common/task_runner_manager.cc",
     "libcef/common/task_runner_manager.h",
+    "libcef/common/workspace_gpu_bridge_export.h",
     "libcef/common/task_util.h",
     "libcef/common/thread_impl.cc",
     "libcef/common/thread_impl.h",
diff --git cef/libcef/common/mojom/cef.mojom cef/libcef/common/mojom/cef.mojom
index 02f180f..64ca442 100644
--- cef/libcef/common/mojom/cef.mojom
+++ cef/libcef/common/mojom/cef.mojom
@@ -93,6 +93,18 @@ interface BrowserFrame {
 
   // Draggable regions have updated.
   UpdateDraggableRegions(array<DraggableRegionEntry>? regions);
+
+  // Release the browser-side hold for a previously acquired workspace surface
+  // revision. |renderer_dissociated| must only be true after the renderer
+  // mailbox/texture association has been torn down.
+  ReleaseWorkspaceGpuBridgeSurface(
+      string surface_id, string revision, bool renderer_dissociated);
+
+  // The renderer imported a workspace surface revision and now owns a mailbox
+  // texture association that must dissociate before the browser-side
+  // SharedImage can be destroyed.
+  RetainWorkspaceGpuBridgeSurfaceRenderer(
+      string surface_id, string revision);
 };
 
 struct CrossOriginWhiteListEntry {
diff --git cef/libcef_dll/libcef.lst cef/libcef_dll/libcef.lst
index 1871406..90672f0 100644
--- cef/libcef_dll/libcef.lst
+++ cef/libcef_dll/libcef.lst
@@ -5,5 +5,6 @@
 {
   /* Only export necessary symbols from libcef.so. */
   global: cef_*;
+          mmltk_cef_workspace_gpu_bridge_*;
   local: *;
 };
diff --git cef/libcef/common/workspace_gpu_bridge_export.h cef/libcef/common/workspace_gpu_bridge_export.h
new file mode 100644
--- /dev/null
+++ cef/libcef/common/workspace_gpu_bridge_export.h
@@ -0,0 +1,38 @@
+// Copyright 2026 The Chromium Embedded Framework Authors. All rights reserved.
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#ifndef CEF_LIBCEF_COMMON_WORKSPACE_GPU_BRIDGE_EXPORT_H_
+#define CEF_LIBCEF_COMMON_WORKSPACE_GPU_BRIDGE_EXPORT_H_
+#pragma once
+
+#include <stddef.h>
+#include <stdint.h>
+
+#include "include/internal/cef_export.h"
+#include "include/internal/cef_string.h"
+
+extern "C" CEF_EXPORT bool mmltk_cef_workspace_gpu_bridge_export_shared_image(
+    const char* surface_id, size_t surface_id_length, const char* revision,
+    size_t revision_length, const char* publication_source_json,
+    size_t publication_source_json_length,
+    CefString* exported_shared_image_json_out,
+    CefString* result_code, CefString* result_detail) noexcept;
+extern "C" CEF_EXPORT bool
+mmltk_cef_workspace_gpu_bridge_renderer_helper_present() noexcept;
+extern "C" CEF_EXPORT bool mmltk_cef_workspace_gpu_bridge_import_texture(
+    void* device, const char* surface_id,
+    size_t surface_id_length, const char* revision, size_t revision_length,
+    const char* exported_shared_image_json,
+    size_t exported_shared_image_json_length,
+    void** texture_out, CefString* result_code,
+    CefString* result_detail) noexcept;
+extern "C" CEF_EXPORT bool mmltk_cef_workspace_gpu_bridge_release_surface(
+    const char* surface_id, size_t surface_id_length, const char* revision,
+    size_t revision_length, bool renderer_dissociated, CefString* result_code,
+    CefString* result_detail) noexcept;
+extern "C" CEF_EXPORT bool mmltk_cef_workspace_gpu_bridge_release_texture(
+    const char* surface_id, size_t surface_id_length, const char* revision,
+    size_t revision_length, CefString* result_code,
+    CefString* result_detail) noexcept;
+#endif  // CEF_LIBCEF_COMMON_WORKSPACE_GPU_BRIDGE_EXPORT_H_
